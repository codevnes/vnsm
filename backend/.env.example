# Server Configuration
PORT=3001

# Database Configuration (MySQL)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_db_password
DB_NAME=vnsm_db
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your_jwt_secret_key
JWT_EXPIRES_IN=1d

# Add other environment variables as needed
# e.g., EMAIL_SERVICE_USER, EMAIL_SERVICE_PASS for forgot password functionality 